---
const titles = [
  {
    name: "Wave Beta",
    platform: "Meta Quest Store | VR Social",
    year: "2024",
    company: "WaveXR",
    image: "/images/wavexr-thumb.png"
  },
  {
    name: "Wave Standalone",
    platform: "PICO Store | XR Experience",
    year: "2023",
    company: "WaveXR",
    image: "/images/wavexr-thumb.png"
  },
  {
    name: "Calvin Harris - The Virtual Experience",
    platform: "Virtual Concert",
    year: "2023",
    company: "WaveXR",
    image: "/images/CalvinHarris_Experience.png",
    youtubeId: "9BncRHR98tk"
  },
  {
    name: "Justin Bieber - An Interactive Virtual Experience",
    platform: "Virtual Concert | Meta Quest, PCVR",
    year: "2021",
    company: "WaveXR",
    image: "/images/Bieber_Wave.jpg",
    youtubeId: "UAhGvhvcoyY"
  },
  {
    name: "Pentakill: Lost Chapter",
    platform: "Virtual Concert | League of Legends",
    year: "2021",
    company: "WaveXR",
    image: "/images/Pentakill_Wave.webp",
    youtubeId: "_rsW0a7JmBs"
  },
  {
    name: "Wave Beta (Legacy)",
    platform: "Meta Quest Store | VR Social",
    year: "2021",
    company: "WaveXR",
    image: "/images/wavexr-thumb.png"
  },
  {
    name: "John Legend - Bigger Love",
    platform: "Virtual Concert",
    year: "2020",
    company: "WaveXR",
    image: "/images/JohnLegend_Wave.png",
    youtubeId: "eGy64l9Yuuw"
  },
  {
    name: "Tinashe - Open Session",
    platform: "Virtual Concert",
    year: "2020",
    company: "WaveXR",
    image: "/images/Tinashe_Wave.png",
    youtubeId: "gxCEmbX-Xdk"
  },
  {
    name: "The Weeknd Experience",
    platform: "Virtual Concert",
    year: "2020",
    company: "WaveXR",
    image: "/images/Weekend_Wave.webp",
    youtubeId: "uViueiV8fME"
  },
  {
    name: "Eden Falling",
    platform: "PC, Turn-based RPG",
    year: "2020",
    company: "Razor Edge Games",
    image: "/images/razor-thumb.png"
  },
  {
    name: "Alimentary Avengers",
    platform: "Mobile (iOS/Android)",
    year: "2015",
    company: "Smartcloud",
    image: "/images/smartcloud-thumb.png"
  },
  {
    name: "Tafe Planetary Plus",
    platform: "Education / VR",
    year: "2014",
    company: "Smartcloud",
    image: "/images/smartcloud-thumb.png"
  }
];
---
<section class="shipped" id="shipped">
  <h2>Shipped Shows & Apps</h2>
  <div class="grid">
    {titles.map(title => (
      <div class={`title-card ${title.youtubeId ? 'has-video' : ''}`} data-video-id={title.youtubeId}>
        <div class="image-container">
          <img src={title.image} alt={title.name} loading="lazy" />
          {title.youtubeId && (
            <div class="play-overlay">
              <div class="play-button">
                <svg viewBox="0 0 24 24" fill="currentColor">
                  <path d="M8 5v14l11-7z"/>
                </svg>
              </div>
            </div>
          )}
        </div>
        <div class="info">
          <h3>{title.name}</h3>
          <p>{title.platform} | {title.year}</p>
          <span class="company">{title.company}</span>
        </div>
      </div>
    ))}
  </div>

  <div id="video-modal" class="modal">
    <div class="modal-backdrop"></div>
    <div class="modal-content">
      <button class="close-modal">&times;</button>
      <div class="video-wrapper">
        <div id="player"></div>
      </div>
    </div>
  </div>
</section>

<script>
  const modal = document.getElementById('video-modal');
  const cards = document.querySelectorAll('.title-card.has-video');
  const closeBtn = document.querySelector('.close-modal');
  const backdrop = document.querySelector('.modal-backdrop');
  const playerContainer = document.getElementById('player');

  const openModal = (videoId) => {
    if (!modal || !playerContainer) return;
    
    playerContainer.innerHTML = `
      <iframe 
        width="100%" 
        height="100%" 
        src="https://www.youtube.com/embed/${videoId}?autoplay=1" 
        title="YouTube video player" 
        frameborder="0" 
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
        allowfullscreen>
      </iframe>
    `;
    
    modal.classList.add('active');
    document.body.style.overflow = 'hidden';
  };

  const closeModal = () => {
    if (!modal || !playerContainer) return;
    modal.classList.remove('active');
    playerContainer.innerHTML = '';
    document.body.style.overflow = '';
  };

  cards.forEach(card => {
    card.addEventListener('click', () => {
      const videoId = card.getAttribute('data-video-id');
      if (videoId) openModal(videoId);
    });
  });

  if (closeBtn) closeBtn.addEventListener('click', closeModal);
  if (backdrop) backdrop.addEventListener('click', closeModal);

  // Close on ESC
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeModal();
  });
</script>

<style>
  .shipped {
    margin: 0 0 120px;
    padding: 0;
  }
  h2 {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--text-muted);
    border-bottom: 1px solid var(--border);
    padding-bottom: 10px;
    margin-bottom: 60px;
  }
  .grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 30px;
  }
  @media (max-width: 900px) {
    .grid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 600px) {
    .grid { grid-template-columns: 1fr; }
  }
  .title-card {
    transition: var(--transition);
  }
  .title-card.has-video {
    cursor: pointer;
  }
  .image-container {
    position: relative;
    width: 100%;
    aspect-ratio: 1;
    overflow: hidden;
    border-radius: 4px;
    margin-bottom: 15px;
    border: 1px solid var(--border);
  }
  .image-container img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: var(--transition);
    filter: grayscale(20%);
  }
  
  .play-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.4);
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: var(--transition);
  }

  .play-button {
    width: 60px;
    height: 60px;
    background: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(4px);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    transform: scale(0.8);
    transition: var(--transition);
  }

  .play-button svg {
    width: 30px;
    height: 30px;
    margin-left: 4px;
  }

  .title-card:hover .image-container img {
    transform: scale(1.05);
    filter: grayscale(0%);
  }

  .title-card:hover .play-overlay {
    opacity: 1;
  }

  .title-card:hover .play-button {
    transform: scale(1);
    background: var(--accent);
    color: var(--bg);
  }

  h3 {
    margin: 0;
    font-size: 1rem;
    font-weight: 700;
    line-height: 1.4;
  }
  p {
    margin: 5px 0;
    font-size: 0.85rem;
    color: var(--text-muted);
  }
  .company {
    font-size: 0.75rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text);
    opacity: 0.6;
  }

  /* Modal Styles */
  .modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
    visibility: hidden;
    opacity: 0;
    transition: var(--transition);
  }

  .modal.active {
    visibility: visible;
    opacity: 1;
  }

  .modal-backdrop {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.9);
    backdrop-filter: blur(8px);
  }

  .modal-content {
    position: relative;
    width: 90%;
    max-width: 1000px;
    background: #000;
    z-index: 1001;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 0 50px rgba(0, 0, 0, 0.5);
  }

  .video-wrapper {
    position: relative;
    padding-bottom: 56.25%; /* 16:9 aspect ratio */
    height: 0;
  }

  .video-wrapper :global(iframe) {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  .close-modal {
    position: absolute;
    top: -50px;
    right: 0;
    background: none;
    border: none;
    color: white;
    font-size: 2.5rem;
    cursor: pointer;
    line-height: 1;
    transition: var(--transition);
  }

  .close-modal:hover {
    color: var(--accent);
    transform: scale(1.1);
  }

  @media (max-width: 768px) {
    .close-modal {
      top: 10px;
      right: 10px;
      z-index: 1002;
      background: rgba(0,0,0,0.5);
      border-radius: 50%;
      width: 40px;
      height: 40px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
    }
  }
</style>
